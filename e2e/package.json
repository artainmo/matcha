{
  "name": "matcha-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress E2E suite for the Matcha project, covering every point of the 42 correction sheet (correction.pdf). Can run either fully dockerized (no local Node.js needed) or natively with a local Node.js >=24 install against the dockerized app.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run"
  },
  "devDependencies": {
    "cypress": "15.20.0",
    "typescript": "^5.7.3"
  }
}
